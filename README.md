# Booper Discord Bot

A Discord bot built with Python and managed using systemd.

## How to run (development)
pip install -r requirements.txt
python bot.py

## How to run (production)
Managed by systemd as `booper.service`.
